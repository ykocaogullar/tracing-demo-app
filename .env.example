# OpenAI API key used by the app to call the chat completions API.
OPENAI_API_KEY=sk-...

# Confident AI API key. Set this to send deepeval traces to the Confident AI
# Observatory. Get one at https://app.confident-ai.com (or run `deepeval login`).
CONFIDENT_API_KEY=
